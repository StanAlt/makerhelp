'use client'

// This component is deprecated. Use SubmitSettingModal instead.
// Kept for backwards compatibility — will not render anything.

export function SettingsSubmitForm({ userId: _userId }: { userId: string }) {
  return null
}
